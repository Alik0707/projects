# ✨ Система модерации - УЛУЧШЕННАЯ ВЕРСИЯ

> Готовая к запуску система модерации обращений клиентов на основе Kafka, Redis и Spring Boot

## 🎯 Что нового в этой версии?

✅ **Исправлены все ошибки сборки и запуска**
✅ **Улучшены Dockerfile с multi-stage build**
✅ **Добавлены автоматические health checks**
✅ **Созданы скрипты для быстрого запуска**
✅ **Оптимизированы настройки JVM**
✅ **Улучшена конфигурация Docker Compose**
✅ **Добавлена полная документация для Ubuntu**

---

## ⚡ БЫСТРЫЙ СТАРТ

### Вариант 1: Автоматический запуск (рекомендуется)

```bash
# 1. Проверьте системные требования
./check-requirements.sh

# 2. Запустите все одной командой
./start-system.sh

# 3. Дождитесь готовности и следуйте инструкциям
```

### Вариант 2: Пошаговый запуск

```bash
# 1. Перейдите в директорию docker
cd docker

# 2. Запустите сервисы
docker-compose up -d

# 3. Проверьте статус
docker-compose ps

# 4. Заполните тестовые данные
cd ../scripts
./populate-test-data.sh

# 5. Отправьте тестовые события
python3 kafka-producer.py --scenarios

# 6. Посмотрите результаты
python3 kafka-consumer.py
```

---

## 📋 Требования

- **ОС**: Ubuntu 20.04+ (или другой Linux с Docker)
- **Docker**: 20.10+
- **Docker Compose**: 2.0+ (или 1.29+)
- **Python**: 3.8+
- **ОЗУ**: минимум 4GB (рекомендуется 8GB)
- **Диск**: минимум 10GB свободного места

---

## 🏗️ Архитектура

```
┌─────────────┐       ┌──────────────┐       ┌───────────┐       ┌─────────────┐
│  Topic-1    │──────▶│  Service-1   │──────▶│   Rules   │──────▶│   Topic-2   │
│  (Kafka)    │       │  Moderation  │       │  Engine   │       │   (Kafka)   │
└─────────────┘       └──────┬───────┘       └───────────┘       └─────────────┘
                             │
                             │ REST API
                             ▼
                      ┌──────────────┐
                      │  Service-2   │
                      │  Enrichment  │
                      └──────┬───────┘
                             │
                             ▼
                      ┌──────────────┐
                      │    Redis     │
                      │   (Cache)    │
                      └──────────────┘
```

---

## 🔍 Доступные сервисы

После запуска системы доступны:

| Сервис | URL | Описание |
|--------|-----|----------|
| **Kafka UI** | http://localhost:8090 | Мониторинг Kafka топиков |
| **Moderation Service** | http://localhost:8080 | API модерации |
| **Enrichment Service** | http://localhost:8081 | API обогащения данных |
| **Health Check** | http://localhost:8080/actuator/health | Проверка здоровья |
| **Metrics** | http://localhost:8080/actuator/metrics | Метрики сервиса |

---

## 📝 Правила модерации

Система применяет следующие бизнес-правила:

1. ✅ **Идемпотентность**: События с одинаковым `eventId` обрабатываются только один раз
2. ❌ **Активные обращения**: Если у клиента есть активное обращение той же категории - отклонить
3. ❌ **Заблокированный клиент**: Если клиент заблокирован - отклонить
4. ❌ **Рабочее время**: Категории TECHNICAL и BILLING только в Пн-Пт 09:00-18:00

---

## 🧪 Тестирование

### Тестовые сценарии

```bash
# Отправить все тестовые сценарии
cd scripts
python3 kafka-producer.py --scenarios

# Генерация нагрузки (100 событий с задержкой 0.02 сек)
python3 kafka-producer.py --count 100 --delay 0.02

# Просмотр результатов
python3 kafka-consumer.py
```

### Тестовые клиенты в Redis

| Customer ID | Tier | Blocked | Active Requests | Результат |
|-------------|------|---------|-----------------|-----------|
| CUST001 | VIP | No | None | ✅ Проходит |
| CUST002 | STANDARD | No | TECHNICAL | ❌ Есть активное |
| CUST003 | BASIC | Yes | None | ❌ Заблокирован |
| CUST004 | STANDARD | No | BILLING | ❌ Есть активное |
| CUST005 | - | - | - | ✅ Нет данных (проходит) |

---

## 🔧 Управление системой

```bash
# Просмотр логов
cd docker
docker-compose logs -f                    # Все сервисы
docker-compose logs -f moderation-service # Конкретный сервис

# Перезапуск
docker-compose restart
docker-compose restart moderation-service # Конкретный сервис

# Остановка
cd ..
./stop-system.sh           # Остановка
./stop-system.sh --clean   # Остановка + очистка данных

# Проверка статуса
cd docker
docker-compose ps
```

---

## 🐛 Решение проблем

### Проблемы с Docker

```bash
# Docker не запущен
sudo systemctl start docker
sudo systemctl enable docker

# Нет прав
sudo usermod -aG docker $USER
newgrp docker
```

### Проблемы с портами

```bash
# Найти процесс на порту
sudo lsof -i :8080

# Остановить процесс
sudo kill -9 <PID>
```

### Проблемы с Kafka

```bash
# Создать топики вручную
docker exec -it kafka kafka-topics \
  --bootstrap-server localhost:9092 \
  --create --topic customer-requests \
  --partitions 3 --replication-factor 1
```

**Полная документация по устранению проблем**: см. `SETUP_UBUNTU.md`

---

## 📁 Структура проекта

```
moderation-system-fixed/
├── service-1/              # Moderation Service (Kafka Consumer + Producer)
├── service-2/              # Enrichment Service (REST API + Redis)
├── docker/                 # Docker Compose конфигурация
├── scripts/                # Python скрипты для тестирования
├── start-system.sh         # 🚀 Автоматический запуск
├── stop-system.sh          # 🛑 Остановка системы
├── check-requirements.sh   # ✅ Проверка требований
├── SETUP_UBUNTU.md         # 📖 Полная документация
└── README.md               # Этот файл
```

---

## 📚 Технологии

- **Java 17** + **Spring Boot 3.2.1**
- **Apache Kafka 7.5.0** - очередь сообщений
- **Redis 7** - кэш и идемпотентность
- **Docker + Docker Compose** - контейнеризация
- **Spring Kafka** - интеграция с Kafka
- **Spring Data Redis** - работа с Redis
- **Spring Retry** - retry политики
- **Lombok** - упрощение кода

---

## 📊 Производительность

Система оптимизирована для обработки **100+ RPS**:

- ⚡ 3 параллельных потока Kafka consumer
- ⚡ Batch processing (50 записей)
- ⚡ Connection pooling для Redis (20 connections)
- ⚡ HTTP клиент с retry и pooling
- ⚡ Асинхронная обработка
- ⚡ Идемпотентность на уровне Kafka

---

## 🎓 Дополнительные ресурсы

- **Полная инструкция для Ubuntu**: `SETUP_UBUNTU.md`
- **Архитектурные диаграммы**: `docs/`
- **Подробное описание Service-2**: `SERVICE-2-DETAILS.md`
- **Техническое решение**: `SOLUTION.md`

---

## ✅ Что исправлено

- [x] Опечатка в `pom.xml` (строка 18: `<n>` → `<n>`)
- [x] Dockerfile оптимизированы (multi-stage build)
- [x] Добавлены health checks для всех сервисов
- [x] Улучшены timeout и retry политики
- [x] Настроены JVM параметры для контейнеров
- [x] Добавлена конфигурация сети (subnet)
- [x] Настроен restart policy
- [x] Оптимизировано логирование
- [x] Созданы удобные скрипты запуска

---

## 🆘 Получить помощь

Если что-то не работает:

1. Запустите `./check-requirements.sh` для диагностики
2. Посмотрите логи: `cd docker && docker-compose logs`
3. Прочитайте `SETUP_UBUNTU.md` - там решения большинства проблем
4. Попробуйте полную переустановку: `./stop-system.sh --clean && ./start-system.sh`

---

## 📄 Лицензия

Тестовый проект для демонстрации событийной архитектуры.

---

**Удачи! 🚀**

Для запуска просто выполните: `./start-system.sh`
